name: patchcore
model_key: patchcore
description: Historical anchor model and strong retrieval baseline.
params:
  backbone: wide_resnet50_2
  layers: [layer2, layer3]
  coreset_sampling_ratio: 0.1
  num_neighbors: 9
trainer:
  max_epochs: 1
