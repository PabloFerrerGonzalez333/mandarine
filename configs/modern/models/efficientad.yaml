name: efficientad
model_key: efficientad
description: Lightweight student-teacher model with low inference latency.
params:
  model_size: small
  teacher_out_channels: 384
  lr: 0.0001
  weight_decay: 0.00001
  padding: false
  pad_maps: true
datamodule:
  train_batch_size: 1
  eval_batch_size: 1
trainer:
  max_epochs: 20
